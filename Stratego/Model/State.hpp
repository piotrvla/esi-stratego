#ifndef STATE_H
#define STATE_H
/**
 * @brief The State enum represents all the states of the game.
 */
enum class State{
    NOT_STARTED,
    NEXT_PLAYER,
    GAME_OVER,
    MOVING,
    SWAPING
};
#endif // STATE_H
