#ifndef DIRECTION_H
#define DIRECTION_H
/**
 * @brief The Direction enum represents every possible direction to move.
 */
enum Direction{
    TOP,
    BOTTOM,
    RIGHT,
    LEFT
};
#endif // DIRECTION_H
