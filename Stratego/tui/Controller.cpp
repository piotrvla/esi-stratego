#include "Controller.h"
void stratego::Controller::start(){
/*
    while(!facade.isGameOver()){
        switch(facade.getState()){
        case State::MOVING:{
            std::string elo = view.askMove();
        }

        };



    }
    view.displayWinner();
*/
}
